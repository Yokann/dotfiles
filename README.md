# Dotfiles

## Hyprland config

### Main dependencies
- walker
- waybar
- swawnc

### Install Hyprland setup

1. Clone repo at `$HOME/.dotfiles`
2. export env vars `DOTFILES_PATH`
3. Run `~/.dotfiles/setup/hyprland/install.sh`

>[!WARNING]
> This setup is used to be run on a clean machine.

